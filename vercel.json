{
  "version": 2,
  "installCommand": "cd web && npm install --legacy-peer-deps",
  "buildCommand": "cd web && npm run build",
  "outputDirectory": "web/.next",
  "framework": "nextjs",
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"gh-pages\" ]; then exit 0; else git diff --quiet HEAD^ HEAD -- web/ backend/ .github/; fi"
}
